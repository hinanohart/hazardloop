# Secrets / credentials (R11: never commit token/key material)
.env
.env.*
*.pem
*.key
*_token
*.secret
.hf_token

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
htmlcov/

# Quarantined failures (R8: failures go here, never rm -rf)
experiments/_wip/

# Large trajectory dumps (never bundle raw agent logs into the repo)
*.traj
*.traj.json
data/raw/
data/cache/
*.parquet

# OS
.DS_Store

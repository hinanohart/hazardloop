"""Trajectory adapters: a backend protocol plus a deterministic synthetic ``mock`` source
and a read-only ``SWE-smith-trajectories`` source. Raw agent logs are never bundled into
the repository; only derived survival statistics are committed."""

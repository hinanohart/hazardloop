"""hazardloop — censoring-aware competing-risk survival analysis for LLM-agent trajectories.

Public API is assembled incrementally as the core modules land (see the build protocol).
``__version__`` is the single source of truth for the package version.
"""

__version__ = "0.1.0a1"

__all__ = ["__version__"]
